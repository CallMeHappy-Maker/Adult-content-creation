/public/css/styles.css — Polished Front-End
Copy code
Css
/* ------------------------------
   General Body & Layout
------------------------------ */
body {
  font-family: 'Helvetica Neue', Arial, sans-serif;
  margin: 0;
  padding: 0;
  background: #121212;
  color: #eee;
}

header {
  padding: 1rem;
  background: #1f1f1f;
  border-bottom: 1px solid #333;
  position: sticky;
  top: 0;
  z-index: 10;
}

header h1 {
  margin: 0;
  font-size: 1.6rem;
}

header p {
  margin: 0.25rem 0 0;
  font-size: 0.9rem;
  color: #aaa;
}

main {
  padding: 1rem;
}

/* ------------------------------
   Feed Section
------------------------------ */
#feed {
  margin-bottom: 2rem;
}

#feed select {
  margin-bottom: 1rem;
  padding: 0.25rem 0.5rem;
  border-radius: 4px;
  border: none;
  background: #222;
  color: #eee;
}

.post {
  background: #1e1e1e;
  padding: 1rem;
  border-radius: 8px;
  margin-bottom: 1rem;
  border-left: 4px solid #0ff;
  word-wrap: break-word;
}

.post strong {
  display: block;
  font-size: 0.95rem;
  margin-bottom: 0.25rem;
  color: #0ff;
}

.post small {
  display: block;
  margin-top: 0.5rem;
  font-size: 0.8rem;
  color: #888;
}

/* Visibility color coding */
.post small:contains("Public") { color: #0f0; }
.post small:contains("Followers Only") { color: #ff0; }
.post small:contains("Private") { color: #f55; }

/* Media styling */
img, video {
  margin-top: 0.5rem;
  max-width: 100%;
  border-radius: 6px;
  display: block;
}

/* ------------------------------
   Creator Section
------------------------------ */
#creator {
  background: #1a1a1a;
  padding: 1rem;
  border-radius: 8px;
  margin-bottom: 2rem;
}

#creator input[type="text"],
#creator textarea,
#creator select,
#creator input[type="file"] {
  width: 100%;
  padding: 0.5rem;
  margin-bottom: 0.75rem;
  border-radius: 6px;
  border: none;
  background: #222;
  color: #eee;
}

#creator button {
  padding: 0.5rem 1rem;
  border: none;
  border-radius: 6px;
  background: #0ff;
  color: #121212;
  font-weight: bold;
  cursor: pointer;
  transition: background 0.2s;
}

#creator button:hover {
  background: #0cc;
}

/* Feedback message */
#creator-feedback {
  font-weight: bold;
  margin-top: 0.5rem;
}

/* ------------------------------
   Responsive Design
------------------------------ */
@media (max-width: 600px) {
  header h1 {
    font-size: 1.3rem;
  }
  
  header p {
    font-size: 0.8rem;
  }
  
  #creator textarea {
    height: 100px;
  }
}
✅ What this CSS does:
Dark, modern theme with consistent fonts
Sticky header for mobile scrolling
Card-style posts with spacing, borders, and padding
Visibility color-coding (Public green, Followers yellow, Private red)
Styled media previews for images/videos
Creator inputs full-width, visually clear, and mobile-ready
Button hover effects for better UX
Mobile responsiveness for small screens
Next step:
Replace your current /public/css/styles.css with this file
Keep your existing index.html and app.js as is
Tap Run in Replit and check the look & feel on your phone
Test posts, media, edit/delete, and filter — everything should now look polished and mobile-friendly